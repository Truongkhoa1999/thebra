export type ShippingInfo = {
    address: string,
    city: string,
    postalCode: string,
    country: string,
}
export type ShippingInfoForNonUser = {
    address: string,
    city: string,
    postalCode: string,
    country: string,
    gmail:string
}